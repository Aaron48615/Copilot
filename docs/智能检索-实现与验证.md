# 智能文本检索与源码接入验证

## 与当前 Fork 主分支的整合验证

整合基线是 Fork `main` 的 `a794b7a`，功能来源是 `codex/complete-smart-retrieval` 的 `5873746`。在 `codex/integrate-smart-search` 完成整合验证，目标是用户 Fork `Aaron48615/Copilot` 的 `main`。原作者仓库仅用于读取。

处理了 README、App 和浏览器测试的文本冲突，并检查了自动合并部分的行为兼容性：

- 保留主线的 `LibraryAnswer`、横屏工作台和自我介绍；`content/` 与基线完全一致，没有恢复 Aaron 用户或旧题。
- 保留原拼音检索算法及中文高亮。当前分类负责即时关键词/拼音候选；单个英文或拼音词只做本地快查，避免被语义模型误当作未知问题并发起生成。
- 完整问句通过本地 API 查当前用户全部题库，语义命中可以跨当前分类打开已有回答。不确定时列候选，候选不合适可生成。
- 继续使用共享回答组件显示命中的核心/题内追问，保持两种布局切换后的回答、筛选和代码依据一致。
- 更新旧浏览器测试里已删除的题目 ID；布局测试等待并点击指定题目，避免在 React 延迟搜索尚未刷新时误点自我介绍。

验证：69 项单元/API 测试、15 项浏览器测试、类型检查与构建通过。浏览器测试新增拼音不调用生成、中文高亮、分类过滤、自我介绍和单用户入口检查。构建仍有既有题库包体积提示。

独立 API 的真实本地模型已就绪：自我介绍精确命中和闭包内存泄漏问法返回已有回答；口语外层变量问题返回正确候选。源码索引读取轻购 51 个文件、城市视图 61 个文件。已删除的 Aaron 用户请求被拒绝，没有读取旧向量缓存绕过当前用户题库。

旧版 12 条探索样例在新题库上按原脚本判据计为 8/12。该判据检查标题中的关键词，不等于准确率：路由权限样例返回“菜单隐藏了，但用户输入网址进入页面怎么办？”，语义相关却未命中脚本正则。Vue 原理、HTTP 缓存、图表尺寸的前列候选仍存在漏召回或偏题，因此不能沿用旧版 11/12 的结论；模型阈值未为此次合并重新调参。DeepSeek 请求结构有模拟验证，因未配置 Key，真实云端生成未验证。

以下内容是功能分支此前的实现与历史验证记录。

## 当前版本：本地混合检索，云端仅用于生成

当前工作保留在 `codex/complete-smart-retrieval`，按用户要求不合并主分支。

- 输入框保留，语音相关功能及搜索工具栏已移除。输入稳定 800 ms 查全部当前用户题库，Enter 可立即提交。
- 精确标题/别名直接返回；其余使用中文 BGE-small-zh-v1.5 q8 的本地向量与关键词 RRF 混合检索。明确框架/项目约束候选；两路一致且分差足够时自动打开已有回答，不确定时显示候选。候选均不符合可手动生成。
- Node Worker 使用 CPU 建立并查询各用户的索引。首次需下载 Hugging Face 模型并计算题库向量；模型和向量缓存在 `.cache/semantic`，不提交。题库修改后重启，根据内容哈希重建。加载中或失败先提供词项候选，不隐式调用付费模型。
- 需要补充或跨项目比较时，使用真实源码片段调用 DeepSeek，默认 `deepseek-v4-flash`，`thinking.type=disabled`，服务端保存 Key。旧 OpenRouter 配置仍支持。
- 未配置 Key 不影响本地语义检索；需要生成时显示配置说明。云端模型实际有效性与生成质量仍需真实 Key 验证。

### 本次验证（2026-09-07）

`npm test` 78/78；浏览器测试 12/12；类型检查、构建、`git diff --check` 通过。构建仍有既有题库前端包偏大的提示。依赖审计修复新增间接依赖告警，`npm audit` 为 0。

`node scripts/evaluate-semantic.mjs` 使用真实本地模型，对 12 条完整问句进行探索性评测。索引就绪并预热词项检索后，单次约 12–26 ms，不含首次下载、建索引、800 ms 输入等待、HTTP 传输或云端生成。11 条满足前 3 候选包含预期主题（跨项目样例检查进入生成并选择两个项目）的条件。样例用于调试和调参，不能当作独立测试集或普遍准确率。

仍失败的样例：“浏览器第二次请求同一个资源，什么情况下不用再下载完整内容？”未把 HTTP 缓存题召回到前 3；显示候选，没有自动跳到错误答案。小型向量模型不是完整语义推理，保留“不符合，生成”入口，并在评测脚本中保留此样例。相似度阈值是启发式，不是概率；更多真实问法仍可能误匹配。

参考：[中文 BGE 模型](https://huggingface.co/BAAI/bge-small-zh-v1.5)、[ONNX 版本](https://huggingface.co/Xenova/bge-small-zh-v1.5)、[DeepSeek 官方 API](https://api-docs.deepseek.com/)、[关闭思考模式](https://api-docs.deepseek.com/guides/thinking_mode/)。

以下为上一版历史记录，工具栏、逐次调用大模型及旧测试数量已被上述当前版本替代。

2026-09-07，任务分支 `codex/complete-smart-retrieval`，基于上游 `511421d`。

## 已实现

- 移除内置语音采集、转写接口及相关实现，豆包输入法、键盘、粘贴统一走文本输入。
- 输入稳定 800 ms 自动查全部当前用户题库，支持输入法 composition、Enter、暂停、取消和过期响应保护。
- 标题/别名完整匹配直接返回；词项与原检索融合召回，语义核对最多 16 条候选，覆盖不足自动流式生成。
- 题内追问自动定位；候选首项与用户已确认上下文分离，模型只在追问时继承上下文。
- 服务启动时加载两个项目索引，语义阶段产生项目范围及源码查询词，生成收到真实片段与文件行号。
- 普通布局、横屏工作台显示来源片段；停止或失败不会把半截答案标为完整回答。
- `npm run projects:setup` 下载缺失的已配置仓库，不覆盖已有目录；API 只使用服务器定义的用户资料和项目。

## 当前实际源码

| 项目 | 本地目录 | 校验版本 | 可索引文本文件 |
| --- | --- | --- | --- |
| 轻购 | `../mobile-shop` | `75a8afb` | 51 |
| 城市视图 | `../datapilot-rebuild` | `900479a` | 61 |

来源分别是 `https://github.com/imeelinew/mobile-shop`、`https://github.com/imeelinew/datapilot-rebuild`。只关联 `default` 用户，未改动源码。索引过滤后的文件数不是完整仓库覆盖率。

真实源码检索检查逐条将返回片段与文件对应行重新比较，全部一致：

- 购物车数量：`src/views/CartView.vue:56–125`。
- 轻购 AI 请求和密钥边界：`src/ai/providers/openai.ts:1–65`、`functions/ai/chat.js:1–70`。
- 路由/菜单权限：`src/utils/permission.ts:1–48`、`src/pages/RoleManage/index.tsx:111–180`。
- 图表容器尺寸：`src/components/ChartRenderer/index.tsx:1–69`。

索引耗时本机单次约 65 ms，以上源码查询约 1–2 ms。全题库首次检索包含索引准备，约 0.6–0.7 秒；后续固定评测约 5–15 ms。这些数字不包含 800 ms 输入稳定等待，也不是模型生成延迟。

## 验证边界

最终检查：`npm test` 69/69，通过；`npm run test:e2e` 8/8，通过；`npm run build` 通过（保留现有整库包体积警告）；`git diff --check` 通过。

单元/API/检索评测和浏览器测试使用模拟模型，不消耗真实模型额度。24 条固定中文评测只检查主题是否进入前 16 个候选，不等于回答正确率；语义选择仍需模型核对。

当前机器没有 `.env.local` 或 `OPENROUTER_API_KEY`。实际本地 `/api/resolve` 的无 Key 精确命中请求返回 HTTP 200；需要模型的问题返回 HTTP 503 和配置说明。**未完成真实 OpenRouter 联调，不能据此声称真实语义选择、生成质量或首字延迟已通过。**

配置 Key 后需真实验证至少：一个口语题库命中、一个轻购源码问题、一个城市视图源码问题，并检查回答引用是否支持结论。在 `.env.local` 设置 `OPENROUTER_API_KEY` 后运行 `npm run dev`；不应把 Key 提交到仓库。

已知限制：模型可能遗漏候选或错误解读代码，源码检索有数量与长度上限，静态站点仍需要 Node API 服务；构建现有整库前端包仍有体积警告。用户 ID 过滤不是登录鉴权，公开部署需入口访问控制。
