---
id: yingke-normal-testing-build-gap
title: 不足八：缺少可执行测试和明确构建脚本
aliases: [能讲讲项目中的测试和构建脚本的缺口吗？, 关于测试和构建脚本的缺口，能结合当前项目解释一下吗？]
category: yingke
difficulty: 基础
priority: normal
projects: [映刻影视]
keywords: [不足, 单元测试, 构建脚本, 自动化]
---

# 不足八：缺少可执行测试和明确构建脚本

## 核心回答

现在 test 还是默认报错的占位，没有真正的自动化测试，也没配项目级构建命令。修改请求适配、文本截断和分页时，缺少自动检查，容易漏掉问题。

我会先给这些能单独验证的逻辑补单测，再到微信开发者工具或对应环境走完首页、列表、详情，最后补上可重复执行的构建和检查命令。

【这次只读源码，没有实际测试或构建，不能说已经验证通过。】

## 回答要点

- 现在 test 还是默认报错的占位，没有真正的自动化测试，也没配项目级构建命令。
- 我会先给这些能单独验证的逻辑补单测，再到微信开发者工具或对应环境走完首页、列表、详情，最后补上可重复执行的构建和检查命令。
- 这次只读源码，没有实际测试或构建，不能说已经验证通过。

## 面试官可能追问

- 为什么先给 adapter、分页和截断逻辑补测试？
- 有测试脚本与有通过记录有什么区别？

## 代码证据

> **代码依据（不用于口述）**
>
> - [package.json 第 9～16 行](</Users/aaron/CodingPractice/14_uniapp/project2/package.json:9>)：`test` 为默认失败占位，未定义项目构建脚本。
